<%= form_for(@user) do |f| %>

  <div class="field">
    
   First Name<%= f.text_field :first_name %><br>
   Last Name<%= f.text_field :last_name %><br>
   Biography <%= f.text_field :bio_text %><br>
  </div>
  <div class="actions">
    <%= f.submit "Update My Simply!" %>
  </div>
  <% end %>